# Security policy

Report vulnerabilities privately through GitHub's security advisory interface.
Do not open a public vulnerability issue.

The bb HTTP API is a trusted-network interface. It is unauthenticated and can
provide command execution and file access through connected hosts. Exposing it
requires an external authentication and network-security boundary.

Supported versions are the latest published release only.
